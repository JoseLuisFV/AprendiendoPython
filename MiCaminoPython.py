# For anidados
for a in range(3):
      # imprimer de 0 a 2 en la posicion que se encuentre
      # la variable a
      for b in range(3):
            # imprimer de 0 a 2 en la posicion que se encuentre
            # la variable b
            for c in range(3):
                  #imprimer de 0 a 2 en la posicion que se encuentre
                  #la variable c
                  print(f"({a}, {b}, {c})")